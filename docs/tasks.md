# Backlog / Tasks

## Параллельная работа
- Для параллельной разработки с ClaudeAI см. `docs/tasks-claude.md` (границы задач, ветки, PR‑процесс).
- Мой поток задач и границы: `docs/tasks-codex.md`.

Приоритет: P0 (критично), P1 (высокий), P2 (обычный), P3 (желательно)

## Backend
- [ ] P0: CoinGecko интеграция (GET /api/data/coingecko/coin/{id}) с Redis‑кэшем и rate limit
- [ ] P0: Таблица `api_keys` + DAO + лимиты по ключу из БД (Alembic миграция)
- [ ] P1: WebSocket: общий брокер батч‑обновлений (экономия запросов), отписки/исключения
- [ ] P1: Логирование запросов/метрик (uvicorn access + структурные логи)
- [ ] P1: Error handling: глобальный обработчик исключений + коды/сообщения
- [ ] P2: CSV/XLSX Signed URL (заглушка → GCS), валидации, ограничения размера
- [ ] P2: CryptoPanic интеграция (новости + простая ML фильтрация)
- [ ] P2: Конфигурируемые котировки/белый список базовых символов
- [ ] P3: Ограничение частоты по IP/префиксам подсетей (DoS защита)

## Frontend
- [ ] P0: UI подписка на `/ws/ticker` (BTC/ETH) + карточки цен
- [ ] P1: Страница источников `/api/data/sources`, чекбоксы включения/исключения
- [ ] P1: Обработка ошибок API/WS, тосты, retry‑логика
- [ ] P2: Страница анализа символа (дерево данных: ticker + analysis)
- [ ] P2: Тесты Vitest для компонентов (2–3 smoke + интерактив)

## CI/CD & Ops
- [ ] P0: Вынести env переменные в GitHub Environments (dev/prod) + secrets‑per‑env
- [ ] P0: Триггер CD по тэгам `v*` и канареечный деплой
- [ ] P1: Проброс секретов в Cloud Run через `--set-env-vars` из Secrets
- [ ] P1: Отчёты покрытия (pytest‑cov) и суммарный badge
- [ ] P2: Линтеры в PR (required checks), автокомментарии на диф

## Data/ETL
- [ ] P1: Схема БД под исторические котировки (candles) + Alembic
- [ ] P1: Job загрузки HTX свечей (история) с дедупликацией
- [ ] P2: CoinGecko бэч загрузки метаданных монет

## ML
- [ ] P2: Простая инференс‑прослойка к FinGPT (POST /api/llm/predict)
- [ ] P2: Валидации входных данных, тайм‑ауты
- [ ] P3: Метрики инференса (latency/errs) + Prometheus

## QA/Docs
- [ ] P1: Руководство по локальному запуску в Docker Desktop (актуализировать quick‑start)
- [ ] P1: Док страница API (список эндпоинтов + примеры запросов)
- [ ] P2: Диаграмма последовательностей WS/HTTP (plantuml/mermaid)

## Terraform (опционально)
- [ ] P2: Модуль для CI/CD секретов (Secret Manager) и привязка к Cloud Run
- [ ] P2: Outputs для URL сервисов (использовать в CD)
