import "@jest/globals";

// Увеличиваем timeout для всех тестов
jest.setTimeout(30000);

// Глобальные настройки для тестов
beforeAll(() => {
  // Здесь можно добавить глобальную инициализацию
});

afterAll(() => {
  // Здесь можно добавить глобальную очистку
});
